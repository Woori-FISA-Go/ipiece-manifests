apiVersion: v1
kind: Service
metadata:
  name: ipiece-server
  labels:
    app: ipiece-server
spec:
  type: LoadBalancer
  selector:
    app: ipiece-server
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
