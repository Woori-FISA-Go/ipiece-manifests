apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ipiece-backend-dev
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/Woori-FISA-Go/ipiece-manifests.git'
    path: apps/backend/overlays/dev
    targetRevision: develop
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: ipiece-backend
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
